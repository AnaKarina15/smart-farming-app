import 'package:flutter/material.dart';

class ViabilityScreen extends StatelessWidget {
  const ViabilityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Verificar Viabilidad')),
      body: const Center(child: Text('Viability Screen')),
    );
  }
}
