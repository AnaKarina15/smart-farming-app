import 'package:flutter/material.dart';

class PhytoHistoryScreen extends StatelessWidget {
  const PhytoHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Historial Fitosanitario')),
      body: const Center(child: Text('Phyto History Screen')),
    );
  }
}
