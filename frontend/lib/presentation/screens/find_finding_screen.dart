import 'package:flutter/material.dart';

class FindFindingScreen extends StatelessWidget {
  const FindFindingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registrar Hallazgo')),
      body: const Center(child: Text('Find Finding Screen')),
    );
  }
}
